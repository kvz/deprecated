# MySQL credentials of source server.
# note: when this is localhost, /etc/mysql/debian.cnf will be used if possible
DB_USER_SOURCE="root"
DB_PASS_SOURCE="xxxxxxxx"

# MySQL credentials of destination server.
# note: when this is localhost, /etc/mysql/debian.cnf will be used if possible
DB_USER_DEST="root"
DB_PASS_DEST="xxxxxxxx"

# What to clone
DO_SOURCES="0"
DO_PACKAGES="0"
DO_PEARPKG="0"
DO_ACCOUNTS="0"
DO_CONFIG="0"
DO_DATABASE="1"
DO_DIRS="0"